<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:background="#F8FAFC"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    android:clipToPadding="false">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:layout_marginBottom="40dp">

        <!-- Título -->
        <TextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🎯 Tus Recordatorios"
            android:textSize="26sp"
            android:textStyle="bold"
            android:textColor="#166534"
            android:layout_marginBottom="6dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Organiza tus actividades con estilo"
            android:textColor="#64748B"
            android:textSize="14sp"
            android:layout_marginBottom="28dp" />

        <!-- Tarjeta del formulario -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/bg_card_gradient"
            android:elevation="6dp"
            android:padding="24dp"
            android:layout_marginBottom="32dp"
            android:clipToOutline="true">

            <!-- EditText para título de la actividad -->
            <TextView
                android:text="Título de la Actividad"
                android:textStyle="bold"
                android:textColor="#1E293B"
                android:layout_marginBottom="8dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

            <EditText
                android:id="@+id/etTituloActividad"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:hint="Ingresa el título"
                android:inputType="text"
                android:background="@drawable/edit_text_background"
                android:layout_marginBottom="20dp" />

            <!-- Input para días antes -->
            <TextView
                android:text="Días antes de la actividad"
                android:textStyle="bold"
                android:textColor="#1E293B"
                android:layout_marginBottom="8dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

            <EditText
                android:id="@+id/etDiasActividad"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:hint="Ej: 3 días"
                android:inputType="number"
                android:background="@drawable/edit_text_background"
                android:layout_marginBottom="20dp" />

            <!-- Botón Guardar Configuración -->
            <Button
                android:id="@+id/btnGuardarConfig"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Guardar Configuración"
                android:textStyle="bold"
                android:textColor="#FFFFFF"
                android:drawablePadding="8dp"
                android:backgroundTint="#166534"
                android:paddingVertical="14dp" />
        </LinearLayout>

        <!-- Lista de Notificaciones -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:text="📅 Notificaciones Programadas"
            android:textColor="#166534"
            android:textSize="20sp"
            android:textStyle="bold" />

        <!-- RecyclerView para mostrar notificaciones -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_view_notifications"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:nestedScrollingEnabled="false"
            android:layout_marginBottom="32dp" />
    </LinearLayout>
</androidx.core.widget.NestedScrollView>